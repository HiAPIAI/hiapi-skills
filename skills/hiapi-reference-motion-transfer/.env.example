# Template only. The CLI does not automatically load .env files.
# Expose these values through the process environment or a protected secret store.
HIAPI_API_KEY=your_hiapi_api_key_here
HIAPI_BASE_URL=https://api.hiapi.ai
